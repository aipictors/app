query Work($id: ID!) {
  work(id: $id) {
    id
    title
    description
    image {
      id
      downloadURL
    }
    user {
      id
      name
      login
      iconImage {
        id
        downloadURL
      }
    }
    tagNames
    createdAt
    likesCount
    viewsCount
  }
}
